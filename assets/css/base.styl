body {
  font-family: "News Gothic MT", verdana;
  font-size: 12px;
  line-height: 1.5;
}
.pin {
  margin-bottom: 18px;
}
.pin input[type=text] {
  width: 40px;
}
.pin input[type=range] {
  position: relative;
  top: 4px;
}


.fun
  position: absolute
  background-color: rgba(200, 200, 200, 0.8)

.param
  position: absolute
  width: 4px
  height: 4px
  background-color: #000

svg
  position: absolute
  top: 0px
  left: 0px